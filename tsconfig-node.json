{
  "extends": "./tsconfig.json",
  //unfortunately relative paths cannot be used from extended files to apply to subdirectories
  //"include": ["./src", "./test"],
  "compilerOptions": {
    "module": "CommonJS",
    "lib": ["ES2020"]
    //"outDir": "./build"
    //"baseUrl": "./src"
  }
}
