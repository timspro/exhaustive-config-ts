{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ES2020",
    "lib": ["ES2020", "DOM"],
    //below assumes that there is webpack (which is used by create-react-app)
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
    //"baseUrl": "./src"
  }
}
